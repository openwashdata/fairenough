# Get key from DESCRIPTION file

Reads and returns key from DESCRIPTION file

## Usage

``` r
get_key(key = NULL, base_path = NULL)
```

## Arguments

- key:

  Optional key to retrieve specific data (e.g., "datasets")

- base_path:

  Base path for the project

## Value

Metadata list or specific key data if requested
