# Filter files by supported extensions

Filter files by supported extensions

## Usage

``` r
filter_supported_files(file_paths)
```

## Arguments

- file_paths:

  Character vector of file paths

## Value

Character vector of files with supported extensions
