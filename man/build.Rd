% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build.R, R/build_legacy.R
\name{build}
\alias{build}
\title{Build R data package in modern 3-step process}
\usage{
build(check = TRUE, website = TRUE, base_path = NULL, verbose = TRUE)

build(check = TRUE, website = TRUE, base_path = NULL, verbose = TRUE)
}
\arguments{
\item{check}{Whether to run R CMD check (default: TRUE)}

\item{website}{Whether to setup pkgdown website (default: TRUE)}

\item{base_path}{Base path for the project (default: uses get_base_path())}

\item{verbose}{Whether to show detailed messages (default: TRUE)}

\item{step}{Which build step to run: "all" (default), "package", "docs", or "validate"}
}
\value{
Invisibly returns build status

Invisibly returns build status
}
\description{
Orchestrates complete package build using modern R development tools.
Separates package structure, documentation, and validation for better control.

High-level function to build the complete data package.
Applies metadata to DESCRIPTION, generates README, and sets up website.
}
\examples{
\dontrun{
# Full 3-step build
build()

# Just build package structure
build(step = "package")

# Build docs and website only
build(step = "docs")
}
\dontrun{
# Full build with checks
build()

# Quick build without checks
build(check = FALSE)

# Build without website
build(website = FALSE)
}
}
