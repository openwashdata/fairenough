# Validate build completion status

Validate build completion status

## Usage

``` r
validate_build_completed(base_path = NULL)
```

## Arguments

- base_path:

  Base path for the project

## Value

Formatted checklist results
